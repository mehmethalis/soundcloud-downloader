
const initialState={
    fetching: false,
	musicList: [],
	error: {}
}

const music=(state=initialState,action)=>{
    switch (action.type) {
        case action.type:
        default:
            return state;
    }
}

export default music;